import { Component } from 'react';

class Header extends Component {
  render() {
    return (
      <header>
        <input type="text" />
        <button>Search</button>
      </header>
    );
  }
}

export default Header;
